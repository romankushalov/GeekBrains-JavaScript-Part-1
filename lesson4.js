// Задание 1. Функция минимального числа
var a = +prompt("Введите число а:");
var b = +prompt("Введите число b:");
function min(a, b) {
return (a < b) ? a : b;
}
console.log (min (a, b));

// Задание 2. 
